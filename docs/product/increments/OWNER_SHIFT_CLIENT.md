# Persisted roster shift client seam

Status: BUILDING. This supporting W4 increment adds a typed browser client method for the existing roster shift endpoint. It does not introduce a shift editor, scheduling availability, worker authentication or a new server capability. The Portal remains read-only for shifts. The existing requirement for tested international DST gap/fold conversion before a friendly editor is preserved.

## Frozen scope and authority

Base is runtime PR70 candidate 4fe929e220bec4064ceeb771bdd0d86cfcbbbaf0. Curie (/root/allocator_harness_builder) owns only packages/flow-ui/src/rosterClient.ts and test/roster.test.ts in isolated work/owner-shift-client. Author commit 18b6991feb5616140fdaebcd25ae7ab123b06b5a changes exactly those two files. Root owns integration, ledger and CI. Maxwell (/root/allocator_harness_review) independently approved only the client mapping and client-only exit of the broader planning draft (90415615); no other editor design was frozen.

The method accepts the existing RosterShiftInput domain without narrowing offsets, microseconds or PostgreSQL zone labels. Literal null is the only create selector; empty, undefined and malformed update identifiers reject before fetch. It reuses the current transport flags, strict input schema, safe expected-version increment and entity receipt matching. Tenant and actor authority remain the unchanged server/RLS responsibility. No client promise of capacity, exactly-once completion or rollback is added. The existing transport's response.ok behavior is not relabeled strict status/code binding.

Root domain inspection 2f3d3f found no source issue. Builder focused roster tests passed 13/13 (f11e8b) and package types passed (808615). The initial two test failures (98992a) were exact-string fixture property-order mismatches; expected fixtures were corrected to the existing schema serialization without changing production behavior. Root full affected flow-ui regression passed 343/343 in ten files (95aa4b), with one worker and unchanged timeouts. Maxwell independently passed source/domain review, all thirteen roster tests (cb2d69) and nine external adversarial groups (7e005b). These add safe-version limits, case-insensitive receipt binding, malformed selectors, offset ordering, distinct new-request success after invalidation and actual Response stream cancellation. His initial external .ts launcher failed before execution; identical .mts execution passed and the failed launch is retained. Root integrated the exact two blobs as 080b377489553c9060e1822814502178f1366fc3 after source/domain review. Bounded Integration/Runtime accepts only this controlled client seam; exact-candidate CI and Release remain pending.

No SQL, route, credential, provider, UI, lockfile or migration changed. Tests here verify a typed client seam; they do not prove a new database mutation or a hosted user journey. Full W4 and W3-W8 acceptance remain open. The next editor work requires an independently reviewed named-zone conversion and precision contract with actual DST evidence before launch.

The follow-on timezone audit found an existing core inverse helper with gap-collapse and first-fold slot semantics. It is not accepted as the editor converter; preserve current booking engine behavior while reviewing gap rejection and both-fold choice separately. No incidental rewrite is authorized by this increment.
