# Dedicated installation document — implementation checkpoint

The accepted predecessor is PR59, `5206d93cfec907041926ebb5833d3ef909872502`, with both required jobs successful in [CI34561367747](https://github.com/ailuminagency-work/booking-lumin-checkout/actions/runs/34561367747). Independent Runtime review and the bounded draft Release receipt are preserved in the continuation evidence. This is not hosted acceptance.

The next dependency-ready work is the portable document handler and bounded policy reader. The current checkout application already owns the same hosted route for its legacy flow. A dedicated renderer origin/build avoids fallback into that existing application. This increment does not register new Netlify routes or activate any profile.

## Actual ownership

- Curie (`/root/allocator_harness_builder`): portable implementation and builder tests; isolated `codex/wave3-mode-document-builder` from the accepted predecessor.
- Maxwell (`/root/allocator_harness_review`): independent adversarial tests in `codex/wave3-mode-document-verifier`, plus Runtime review after integration.
- Chandrasekhar (`/root/allocator_source_review`): independent architecture, domain and source review.
- Root (`/root`): integration branch `codex/product-mode-document`, shared exports, evidence/ledger, Integration and Release coordination.

These are four actual identities combining roles, not nine different reviewers. Failures return to the builder. Local source/domain and adversarial checks are recorded in [MODE_DOCUMENT_EVIDENCE.md](MODE_DOCUMENT_EVIDENCE.md). Integrated Runtime, candidate CI and Release remain required at this checkpoint.

## Remaining dependencies

This portable subleaf is not completion of T3 or W3. A real public-policy API and guarded database composition, actual HTTPS/SQL/browser tests, dedicated deployment registration and hosted precedence evidence remain required. T4 loader/controller and the separately reviewed public session bridge follow. Full customer workflows, W4 and W5–W8 remain incomplete; W9 real-provider activation is excluded.

Public bootstrap data is an inert snapshot, never authorization. No session creation, booking confirmation, payment action, credentials, migration or deployment is introduced here. Local raw-target checks cannot reconstruct normalization performed by a CDN.

## Frozen proposal revision 5

The following proposal is preserved with its historical approval wording. Original UTF-8 file SHA-256: `2C08D4D499DE18D9BFF852D2F4AD63B29FB70E0EA553344338EEFC63A191F9E0`.

# T3 dedicated document and public-policy adapter proposal - revision 5

Author Curie `/root/allocator_harness_builder`. PROPOSAL ONLY: no implementation authorization, path reservation, migration, deployment or profile activation. Base5206d93cfec907041926ebb5833d3ef909872502, predecessor PR59 verified by root at9d5a7d; exact next-candidate Integration/Runtime/CI/Release remain separate. Read accepted T1 installation contracts, S1 publicPolicy repository/SQL, topology revision3 and mode-owner-next-dependency-audit. All choices below require independent architecture/security and root freeze.

## 1. Executable result and deployment separation

Deliver a real dedicated document handler, real bounded read-only public policy HTTP adapter backed by existing S1 PostgreSQL, and an actual local HTTPS browser harness. The document is intentionally inert: it says “Booking experience is not available yet” and carries only a strictly checked public bootstrap snapshot. It starts no session, form, payment, owner request, message listener or loader. This establishes the secure document boundary for the later T4/controller and separately frozen public session bridge; it does not claim operational customer booking.

Concrete conflict: accepted checkout App already interprets /checkout/flow/:id as legacyHostedFlow and preview rewrites /checkout/* to SPA. Proposed resolution is a DISTINCT dedicated renderer deployment/build/origin R, not insertion in the existing checkout SPA. The accepted demo origin/build/HostedFlow remain unchanged. Root owns eventual Netlify Edge entry/registration and dedicated artifact separation; root must not register these routes over the current demo deployment. No UUID-based new-versus-legacy probing, no fallback on policy404, no context.next on a dedicated-mode request. Dedicated R serves only this document boundary; arbitrary index/assets paths deny until separately registered by T4. The local equivalent owns the entire HTTPS listener and returns terminal errors for all other routes.

One immutable trusted profile per document/API harness instance; zero-profile startup is allowed but all document/policy requests return503 without upstream/DB work. More than one profile rejects startup for this bounded increment, rather than choosing a profile from the response. Profile includes version/R/A/P/loader. T1 grammar plus S1 ASCII DNS non-ACE subset applies: no IP hosts or xn-- labels, numeric/hex alias final labels excluded exactly as accepted S1 contract. R,A,P pairwise distinct; all test M origins distinct from these. Full SQL registry-to-compiled profile reconciliation, including P omitted by the public DTO, is trusted fixture/deployment work, not something inferred from publicPolicy.

## 2. Observable URL and request boundary

Dedicated document methods GET and HEAD only. Exact paths /checkout/flow/<canonical lowercase UUID> and /embed/flow/<canonical lowercase UUID>, via accepted parseInstallationRoute. Raw local request target max256 ASCII bytes, origin-form only; no query including empty ?, fragment including empty #, percent, backslash, control/nonASCII, duplicate slash, dot segments, trailing slash or additional segment. Recognized prefixes with malformed suffix return400; other paths including /index.html and /checkout/index.html return404. Unknown paths never reach policy. Unsupported methods return405 with Allow: GET, HEAD; malformed target400 takes precedence. Well-formed unknown/disabled/mode-mismatched installation404. No redirect response, canonicalization or SPA aliases.

Require exact configured HTTPS authority at local TLS Host and reject duplicate Host; no absolute-form target, credentials, forwarded-host rewrite or request-selected origin. Local SNI is the configured DNS name. HEAD performs identical lookup/validation and emits the same status/security/content-type/content-length as GET, with zero body bytes. Ignore conditional validators/Range for document semantics: no304/206, all responses are fresh whole responses. Reject nonzero or duplicate Content-Length and any Transfer-Encoding with400; no body consumed as configuration. GET/HEAD may carry normal browser navigation headers. Cookie/Authorization are rejected400, never forwarded or interpreted. Foreign Origin/Referer/Sec-Fetch fields do not choose ancestry or grant authority; requests may omit Origin, as normal document navigation does.

IMPORTANT ingress limitation: Netlify Request.url can already be normalized. The portable adapter validates the observable URL, requires exact configured origin, rejects every query/fragment (including empty delimiters), and requires observable request.url exactly equal configured rendererOrigin plus the accepted canonical path before any fetch; it must not discard search/hash while reconstructing pathname. Observable normalization aliases reject when still represented in request.url; it cannot prove original percent/dot aliases absent upstream. Local Node raw-target tests establish local behavior only. Production CDN raw alias rejection, Edge precedence, direct-index/static fallback and final response headers must be demonstrated at authorized staging before hosting activation. If platform cannot supply/enforce that boundary, RETURN deployment topology; no claim that Request.url reconstructs lost raw bytes.

## 3. Public policy wire endpoint

On dedicated A only, GET/HEAD /api/public/installation-policies/<canonical lowercase UUID>, max256 raw bytes, same malformed-target/Host/body rules. OPTIONS on this exact route returns204 only for Origin exactlyR, Access-Control-Request-Method GET or HEAD, and no requested non-safelisted headers (header absent/empty); otherwise400/403. No auth, owner routes, list/discovery endpoint, profile registration, public sessions, mutation or customer data. Unknown routes404; methods405 Allow: GET, HEAD, OPTIONS. Auth/Cookie reject400. Requests without Origin are permitted for the Edge server reader. Origin present must exactlyR; opaque/null/foreign403. The public nature and spoofability of Origin are explicit; this is not merchant/private authentication.

GET200 body is the unwrapped exact InstallationPolicy object, not owner transport or SQL metadata. Exact keys: schemaVersion,installationId,mode,deploymentProfileVersion,rendererOrigin,apiOrigin,loaderUrl,currentVersionId,targetRevision,policyRevision,allowedParentOrigins,enabled. Require schema1, canonical UUIDs, safe positive revisions, enabledtrue, profile/ID binding and accepted hosted-empty/iframe1..20 sorted unique parents. Reuse createInstallationContracts and S1 result validator; do not reimplement a relaxed parser. Response body max16384 UTF8 bytes including all JSON. No tenant/flow/customer/worker/credential/token fields. HEAD retains GET content-length and emptybody.

Only genuine repository `{kind:completed,delivery:data}` with validated data can yield200. Failed UNAVAILABLE ->404; all other repository failures/withheld/completion_uncertain ->503. A malformed HTTP ID was already400 before repository dispatch. No server error/code/body is reflected. Every public API error body is exact {schemaVersion:1,error:{code:INVALID_REQUEST|NOT_FOUND|METHOD_NOT_ALLOWED|FORBIDDEN|RATE_LIMITED|UNAVAILABLE}}, with corresponding400/404/405/403/429/503. HTTP parser failures may close the connection before a response. Never report a failed read as empty success. No rollback or mutation language is needed on this read-only public wire.

All API responses: Cache-Control:no-store,max-age=0, CDN-Cache-Control:no-store, Netlify-CDN-Cache-Control:no-store, Content-Type:application/json; charset=utf-8 (OPTIONS no body/content type), X-Content-Type-Options:nosniff, Referrer-Policy:no-referrer, Cross-Origin-Resource-Policy:cross-origin. Add Access-Control-Allow-Origin: exactR and Vary: Origin only when authorized Origin was present; no wildcard or Allow-Credentials, no credentials echo. OPTIONS max-age60, explicit allow-method GET, HEAD, allow-headers omitted. No Set-Cookie, ETag, Last-Modified, Location, Server identifying internal services. No compression; Content-Encoding absent. Fixed error cap256 bytes, all HEAD errors empty.

## 4. Real local PG composition and lifetime

New server-only createLocalModePolicyComposition validates a captured immutable environment, not request config. LOCAL_HARNESS=1,FLOW_TEST_DISPOSABLE=1,MODE_INSTALLATIONS_TEST_DISPOSABLE=1,MODE_DOCUMENT_TEST_DISPOSABLE=1. PGHOST exactly127.0.0.1 or localhost, canonical port1..65535, PGUSERpostgres, database ^lumin_mode_document_[a-z0-9_]+$ length<=63. Reject nonempty DATABASE_URL,PGHOSTADDR,PGSERVICE,PGSERVICEFILE,PGPASSFILE,PGOPTIONS. Password absent/empty or exact synthetic postgres only, internally created fixed callback captures validated value to prevent pgpass fallback. No caller credential callback, environment mutation or hosted connection string. Old S1/S2 public factory prefixes remain byte-for-byte unchanged.

One dedicated max2 Pool with SSLfalse,pipelinefalse, connect3000ms,idle5000ms, statement/lock5000ms,idle-transaction1000ms,query_timeout0. Reuse __createModeInstallationRepositoryForTests behind this NEW guarded local composition, exposing only publicPolicy and close. Wrap query interface with exact accepted transaction-command allowlist and fixed SELECT public.mode_public_installation_policy($1::uuid) AS result, preventing accidental owner-method invocation; no handwritten replacement SQL, no new migration/grants. Reuse existing strict read metadata and COMMIT acknowledgement semantics. Trusted fixture seeding uses a separate disposable setup connection, never exposed by adapter. The pool's postgres startup plus SET LOCAL ROLE service_role is local synthetic testing only, not least-privilege production credential certification.

Public-policy HTTP total deadline2000ms from handler entry including admission/DB/serialization, with trusted server-created signal. It can expire before the existing repository's10s/3s budgets; abort repository, return503 if writable, consume all late outcomes. Do not reduce immutable repository deadlines or claim socket cancellation immediately stops SQL. Disconnect likewise aborts; do not write a late response. Retain active repository accounting until its bounded settlement, even if socket closed. Server-side prearmed5s limits remain necessary. Handler max16 total active, max2 unresolved DB reads (429 on additional admission, no unbounded queue beyond owned two pool requests).

Header limit8192, native header timeout2000ms/request timeout2000ms, keepalive1000ms; raw duplicate-sensitive checks. Fixed60s windows:120 policy requests per socket remote address and600 per installation, matching accepted topology. Combined maps max10000 entries, expired windows pruned before insertion, never evict active entries. Count malformed input against address only; installation counters only for canonical IDs. Every admitted request counts even if later missing/unavailable. Retry-After canonical integer1..60 for rate/fullmap (ceil remainingwindow, capacity uses earliestexpiry); concurrency uses1. Reject429 before DB. Fixed-size keys, no credentials/header-provided IP. Clock nonfinite/backward permanently closes admission and aborts active operations; consume late completions. Capture native clock/timer intrinsics and use entry-relative arming/fresh continuation samples. No async telemetry or unbounded error collection.

close() synchronously stops admission, aborts active reads, closes all owned listener sockets and repo/pool concurrently; resolves within10000ms from first close call, idempotent. Bound partial headers and startup rollback if A or R listener bind fails, consume every late rejection. No repeated independent10s phases. Local startup owns two HTTPS listeners and one pool; browser M/P fixture servers are verifier-owned with their own bounded cleanup. No shutdown rollback claim.

## 5. Fixed upstream reader

Portable createModeDocumentHandler receives one trusted profile snapshot and captured fetch/clock platform dependencies at construction; no application-side effects on import. Upstream URL is exactly profile.apiOrigin + /api/public/installation-policies/ + parsed UUID. Origin cannot come from public policy, request Host, query, cookie or redirects. Fetch GET with credentials omit, cache no-store, redirect error, referrerPolicy no-referrer, Accept application/json only. Never forward request headers, Authorization, Cookie, Referer or platform tokens. No browser fetch is performed by T3 HTML.

2000ms total from document handler entry includes parsing/admission/fetch/body/validation/output construction, not2s per phase. Use one AbortController and remaining-time budgets. Max16 unresolved reader operations per instance; additional503 with fixed error. A slot is reserved synchronously before fetch and is not released merely because caller deadline/disconnect produces a terminal503. It remains held until the underlying fetch, active body read and requested cancel/abort cleanup have all settled; late settlements are cleanup-only and can never replace the terminal response. If a trusted fetch/read/cancel never settles, it permanently occupies that slot until instance teardown, so repeated timed-out requests cannot create unbounded hung operations. At most16 retained bounded request contexts. Do not await uncooperative cancellation in the caller response path: race the shared2s deadline, attach late rejection consumers, and retain slot ownership separately. close stops admission, aborts all owned operations, settles waiting responses and returns within10000ms; never promises that arbitrary platform fetch physically stopped. A permanently closed instance cannot be reopened to evade exhausted slots. Edge global distributed rates are not proven by instance cap; A enforces local bounded rates above. A404 maps document404; any other non200, redirect, transport/body failure, unsupported Content-Type, nonidentity Content-Encoding, invalid policy, overflow or timeout maps503. A429 is deliberately503 at document boundary, not confused with unknown installation. A200 must have Content-Type application/json optionally charset=utf-8 only; valid singleton Content-Length if present1..16384 and actual bytes must match. Stream cap16384 before decode/parse, fatalUTF8, abort/cancel reader on earlyfailure. No stale cache/retry/fallback. Portable reader keeps a repository-wide last monotonic sample across calls, with no new baseline after a backward sample. Clock throw/nonfinite/negative/backward sample permanently closes admission, aborts all active operations and settles writable document callers503, while retaining underlying-operation slots until cleanup settles. Future calls503 without fetch even if clock later recovers. Sample deadline after every awaited success/rejection and before final response; late outcomes cleanup-only. Fixed response errors reveal no upstream diagnostics.

Parse network JSON only after byte cap. Classification order: first require exact structural T1 policy/profile and S1 subset validity plus requested installation ID/profile/R/A/loader equality; any such invalidity is503 even if an untrusted enabled field saysfalse. Once that entire structural/binding validation succeeds, enabledfalse or valid stored-mode versus route-mode mismatch is404. Otherwise enabledtrue and matching mode yields200. Thus registry-valid disabled and mode-mismatched policies are unavailable, not malformed; mismatched requested ID/profile/R/A/loader remain503. Trusted constructor objects receive descriptor-based immutable copying, exactkeys/arraylength/byte preflight; reject accessors/symbols/holes/cycles and caught reflection failures, never invoke getters/toJSON/iterators. Do not claim Proxy reflection cannot run traps on Edge: no portable isProxy facility. Hostile values belong to network JSON; platform fetch/clock/native signal internals are trusted. Registry max1 and immutable copied profile; output policy at most20origins. Future production transport identity/credentials are explicitly not invented.

## 6. Complete HTML and exact headers

Successful document status200, Content-Type:text/html; charset=utf-8, bounded32768 UTF8 bytes. HTML is a fixed accessible semantic template: doctype/html lang=en/head charset+viewport+fixed title/main h1 and fixed unavailable paragraph. No tenant branding/customer data/links/forms/event handlers/scripts/styles/images/iframes. Bootstrap is a nonexecuting template element id=lumin-mode-bootstrap whose template.content.textContent is the exact JSON after HTML entity decoding; serialized HTML contains escaped JSON exact {schemaVersion:1,kind:installation_document,operational:false,policy:<validatedPolicy>}. The future consumer reads template.content.textContent, not template.textContent (which is empty for a template). Unit DOM parsing must prove exact JSON roundtrip and zero executable elements, including closing-script/template attack strings that must already fail policy validation. Escape & < > and quotes in text using fixed helper; do not concatenate raw JSON into executable script or attribute. Bootstrap complete JSON<=17408, wholeHTML<=32768; validate lengths before response. No random secret/nonce because there is no executable script. Public bootstrap is snapshot metadata, never proof of current authorization, signed capability or session handle. T4 must refetch current policy and enforce its own protocol; already downloaded snapshots cannot be revoked retroactively.

All document responses: Cache-Control:no-store,max-age=0, CDN-Cache-Control:no-store, Netlify-CDN-Cache-Control:no-store, X-Content-Type-Options:nosniff, Referrer-Policy:no-referrer, Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), Cross-Origin-Resource-Policy:cross-origin. No CORS headers required for navigation, no COEP/COOP requirement introduced, no cookies/validators/redirects. Content-Length is exact final UTF8 body length; HEAD same length/no body. No HSTS/preload on synthetic test hosts; production TLS policy separate.

CSP for successful hosted: default-src 'none'; base-uri 'none'; object-src 'none'; script-src 'none'; style-src 'none'; img-src 'none'; font-src 'none'; connect-src 'none'; frame-src 'none'; form-action 'none'; frame-ancestors 'none'. Also X-Frame-Options:DENY. Successful iframe uses identical directives except frame-ancestors is the space-separated exact validated allowed origins; omit X-Frame-Options so supported cross-origin parents are not unintentionally denied. No wildcard/scheme-only/unsafe-inline/eval or request-derived source. Inert content needs no runtime origin allowances. Errors use same all-none CSP and frame-ancestors 'none'/X-Frame-Options:DENY, fixed accessible HTML with one generic message per400/404/405/503,<=1024bytes; same nosniff/no-store/referrer headers.

CSP checks EVERY ancestor. Nested allowed-parent-under-forbidden-top must be blocked in actual browser; it is not enough to test direct forbidden parent. CSP does not stop top-level navigation to an iframe-mode URL, and this inert document may display there. Direct-parent-only initialization belongs to T4; no false authentication or top-level-navigation rejection claim. Header changes cannot erase already downloaded documents or prevent nonbrowser spoofing.

## 7. Local HTTPS verification implementation

Harness uses pinned existing Playwright/Chromium, CREATE-only fresh all30 PG databases in public/extensions layouts and actual publicPolicy repository. Trusted local TLS certificates with SAN names under reserved .test are generated for disposable fixtures, never production keys. Explicit paths to test cert/key accepted only by local harness guard; no certificate secret in screenshots/logs/artifacts. Local A/R/P/M/M2 DNS names distinct and map only127.0.0.1 through controlled browser resolver and server-side HTTPS test transport lookup. Do not globally disable TLS verification or mutate NODE_TLS_REJECT_UNAUTHORIZED. Use one disposable self-signed RSA2048-or-stronger certificate with exact SAN fixture names and validity limited to the test period, supplied by a root-owned test certificate provisioning tool. Server-side reader test transport uses Node HTTPS with explicit ca equal to that fixture certificate, rejectUnauthorized:true, normal hostname verification and fixed allowlisted .test-to-loopback lookup. The portable production reader still uses captured platform fetch; this Node TLS transport exists only in verifier/local composition. Chromium launches in a new isolated owned process with --ignore-certificate-errors-spki-list=<base64 SHA256 of fixture public-key SPKI DER>, calculated with Node X509Certificate/publicKey.export and createHash. No general --ignore-certificate-errors, ignoreHTTPSErrors, OS trust store change or NODE_TLS override. Pinned-key Chromium bypass is explicitly TLS transport/CSP-origin proof, not browser public-CA or hostname-chain certification; separately prove Node reader rejects wronghostname/untrustedcertificate and a browser endpoint with a different key fails. Loopback resolver rule applies only these exact fixture names, no wildcard/proxy. Root reserves certificate provisioning/cleanup and approved artifact paths before dispatch; private fixturekey never enters repository, logs or artifacts. Redirect/SSRF tests use owned loopback fixtures only; never external domains.

Acceptance cells:
A. Fresh genuine SQL policy both layouts for hosted/iframe, enabled/disabled, archive/inactive tenant/service and unknown; exact bytes/projection, no session/request/booking/outbox changes. Trusted owner setup uses accepted RPCs. Inspect SELECT/COMMIT real execution and no mutation queries; malformed IDs/routes deny before lookup. Profile mismatch/emptyregistry fail closed; unchanged old factories still deny new prefix.
B. Full raw HTTP status/method/HEAD/content length, query/percent/dot/backslash/double-slash/index/fallback/Host/duplicate/body/oversize controls with valid neighbors. Native8192header overflow connection-close control. Policy CORS/OPTIONS/authcookie/no forwarded authority. Real stored origin allowlist yields exact final CSP, not just serializer fixtures.
C. Actual Chromium top-level hosted document; hosted framed by evenapprovedM blocked; iframe under approvedM visible, disallowedM2 blocked; forbiddentop->allowedimmediate->R blocked; permittedallancestors frame may display inert content but no direct-parent session claim. Capture final headers and DOM marker; never count frame-load event alone as CSP proof. No session/API traffic from HTML, no storage/cookies/ownercredentials, no downloaded legacybundle/no SPA session side effect. Combine attempted legacy/new issue/submit call counters, complete relevant unchanged database snapshots and source/import review; unchanged rows alone do not prove absence of rolled-back issuance attempts. Use a distinguishable safe static/index and SPA fallback fixture behind the local boundary and prove no forbidden response contains either marker.
D. Bounded controlled stalled fetch/headers/body, invalidUTF8/contentlength/type/encoding, redirects,16384 boundary and oversizedstream, hostile constructorobjects, actual failurecleanup. Actual blockedSQL + handlerabort/deadline shows503/no late success, retains accounting until repo settles, eventual backend cleanup under armedserver timeout. Late completed policy cannot replace already-returned error. Exact rate/mapcap/prune/noeviction and concurrency controls, timer regression/throw/close/bindfailure. Mark virtualpeer stress versus physical network topology honestly.
E. A read begun before an owner policy update commits may legitimately return its already authorized transaction snapshot; no continuous revocation claim. A new read begun after committed update must not serve the earlier cached policy. Same installation owner updates policy between successive requests: first document remains historical, nextGET usesnewCSP or404, no cache/stalefallback. Targetupdate nextdocument reflectsnewtarget; no sessionpin semantics added or claimed. HEAD failures still failclosed. PublicDTO limits do not prove full SQLP/profile alignment; explicit fixture reconcile does.
F. Legacy checkout/owner/session/financial regressions unchanged, complete unit/types/build/preview/no-contamination controls; independent author separation, exact candidate CI/Runtime/Release. Root Netlify registration static review may land only inert/dedicated configuration; actual deployment/header-precedence acceptance remains blocked until authorized hosting reconciliation.

## 8. Prospective dependency and ownership map

No file reservation from this proposal. Root collision-scan and dispatch exact paths before implementation. Root has selected staged implementation: first portable response/reader only (modeDocument.ts plus its focused test) in an isolated builder; real public-policy adapter/local TLS/browser implementation remains mandatory follow-up under the same parent T3. First subleaf acceptance must explicitly say portable module only, not completed T3 or operational document deployment. No Edge registration/current demo changes in that first dispatch. BuilderA: new packages/flow-ui/src/modeDocument.ts and focusedtest for portable pure response/reader; no pg/Node import in portable entry. BuilderB: new action-api/server/mode-policy-http.ts, mode-policy-local.ts and focusedtests, fixed PGcomposition only. Independent verifier: new tests/mode-document/document.integration.ts, fixtures.ts and browser spec; root may split fixture ownership to keep three activecells. Root exclusively owns Netlify Edge entry/registration, package exports/scripts/CI/dependency/cert provisioning and dedicated build artifact guard. Reuse pinnedbrowser tooling/artifactprivacy controls, but root must reserve a separate document-suite safe reporter/artifact name/schema allowlist; the existing owner-only validator is not implicitly suitable. Keep one worker/no retries/10min runner and10s owned cleanup, no trace/video/automatic screenshots. Repeat four forced sentinel failure controls over all generated files for changed harness paths; fixed diagnostics only. Exact approved paths/count/byte caps must be recorded by root tooling addendum before dispatch rather than widening the old owner allowlist. No edits to migrations0001..0030, acceptedinstallation/session/allocator source, legacyHostedFlow, Portal activation links or old factory guards.

Dependency: acceptedT1+S1+PR59 -> exactT3freeze -> independent builder response/API -> actuallocalHTTPS/SQL/browser -> Runtime/exactCI/Release -> boundedT3localreceipt. Downstream T4/controller/loader -> publicsessionbridge includingrestart/secret/uncertainissuance -> fullcustomer workflows -> distincthostedR/A/P/M and genuineAuth/leastprivilege/deployment reconciliation. Full W3 fields/media/pricing/workflows/runtime plus W4-W8 obligations remain active/incomplete. No local T3 receipt is a wholewave completion.

## 9. Explicit decisions still requiring root resolution

The design deliberately narrows renderer instances to one trusted profile and separate R deployment, keeps documents inert, and uses a new guarded local composition rather than altering existingprefixes. These are proposed choices for approval, not silent product scope waivers. Root has accepted dedicated renderer origin/build separation and process-scoped pinned-key browser TLS for this proposal. Exact local trust implementation is fixed in section7. Hosted Netlify raw-ingress/precedence remains an explicit acceptance blocker, not a reason to substitute legacy SPA routing. Local implementations can prove adapter behavior while hosted raw-path/Edge precedence remains expressly blocked, but they must not silently reuse the current demo origin. Genuine hosted public API credentials/network, migrations0001..0030 reconciliation and provider activation remain outside authorization.
